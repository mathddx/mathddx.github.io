---
title: "Guia de setup do site"
date: 2026-06-23
categories: [Código]
tags: [Tutorial]
#author: "Leo"
---

## Dia 23/06/2026

---
> [Site](https://chirpy.cotes.page) para tópicos recorrentes de formatação MD
{: .prompt-info }


Ao invés de fazer relatórios e estudar para as provas, o divertido foi fazer coisas loucas aqui nesse site. Em especial, conseguir colocar ele pra rodar no Thinkpad, e pra isso foi feito o seguinte:

Começando, uma vez que estamos utilizando Arch Linux, foram utilizados os seguintes comandos para instalar as dependências do  [Jekyll](https://jekyllrb.com/docs/installation/ubuntu/):
```sh
sudo pacman -S ruby base-devel ruby-erb
```
Após isso, deve se adicionar as gems ao PATH do ~/.bashrc, a partir das seguintes linhas:

```sh
export GEM_HOME=$HOME/gems
export PATH=$HOME/gems/bin:$PATH
export PATH="$HOME/.local/share/gem/ruby/3.4.0/bin:$PATH"
```
E depois atualizar o bash por meio de `source ~/.bashrc`

Após isso, deve-se clonar o repositório e instalando as dependências do bundler:

```sh
gem install jekyll bundler
```

> O comando acima deve ser usado sempre que a pasta for baixada de novo do github, então é bom deixar em apenas um computador.
{: .prompt-tip }

Além disso, caso o instalador do gem não funcione, basta fazer isso:
```sh
export PATH="$HOME/.local/share/gem/ruby/$(ruby -e 'puts RUBY_VERSION.sub(/\d+$/, "0")')/bin:$PATH"
```
```sh
export GEM_HOME="$HOME/.local/share/gem/ruby/3.4.0"
export PATH="$HOME/.local/share/gem/ruby/3.4.0/bin:$PATH"
bundle install
```

Para evitar o acúmulo de arquivos desnecessários, deve-se rodar o seguinte:
```sh
gem cleanup
```

Para configurar o perfil do github, alguns probleminhas com auth-keys ocorreram, mas forram resolvidos configurando pelo site a partir de:

```sh
sudo pacman -S github-cli
gh auth login
```

No mais, os seguintes comandos são usados:

### Iniciar site local:
```sh
bundle exec jekyll s
```
#### Colocar no github:
```sh
 git add .
 git commit -m 'comentário'
 git push origin main

```
Para adicionar no github após baixar o site novamente, foi feito o login pelo navegador, e depois adicionado o seguinte comando:

```sh
git remote set-url origin git@github.com:mathddx/mathddx.github.io.git
```
Com isso, o site foi atualizado normalmente a partir dos comandos padrões do github.

Site atualizado por último no *PC*
E com alguns problemas de deploy, corrigi apenas adicionando essa linha e refazendo o processo....





Para combinar os dois SSDs, apenas usei o lsblk pra ver qual era qual, e utilizei essa linha de código para combinar os dois

```sh
sudo btrfs device add /dev/sda /mnt
```